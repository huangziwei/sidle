.claude
.DS_Store

ref/
books/
bench
scripts/
boko-kai/target/
/target
sidle/src-tauri/gen/

artifacts/

# Per-install secret + binary. The .example file is the template; real
# values fill in `etc/server.conf` at install time and stay out of git.
kual/sidle/etc/server.conf
kual/sidle/bin/sidle

# build.sh staging: the sidle-server sidecar and KUAL resources copied here for
# `cargo tauri build` to fold into the bundle. Regenerated each build; never committed.
sidle/src-tauri/binaries/
sidle/src-tauri/resources/

# Per-machine vendored 3rd-party binaries (not source). Previously the
# bblanchon libpdfium cache; the PDF engine is now macOS PDFKit (no bundled
# dylib), so this is only kept ignored for any stale cache from older builds.
vendor/